# 진안사우나(@jinan.sauna) 스레드 자동 포스팅 앱 — 종합 매뉴얼

> 구축일: 2026년 9월 | 상태: **설치 대기 (README.md 의 최초 설정 6단계 진행 필요)**
> 저장소(예정): `github.com/chinyangwoo/jinan-sauna-threads`
> 원본: 홍삼빌호텔 스레드 자동 포스팅 앱과 동일 로직 (하루 3회, 상품·계정만 변경)

---

## 1. 이 앱이 하는 일 (한 줄 요약)

사장님이 사진만 폴더에 넣어두면, Claude AI가 MZ 감성의 글을 쓰고 랜덤 사진 3장을 골라 **매일 3회 @jinan.sauna 스레드에 자동 게시**합니다. 소개 상품은 **진안 사우나투어**(www.jinansauna.kr): 핀란드 배럴 사우나 + 토도노이 냉수욕 + 마이산 3km 사우나러닝 + 홍삼빌호텔 1박 + 성수주조장 딸기막걸리 시음 + 제철 조식, 1인 99,000원(월~목 한정·성수기 제외). 서버 비용 0원, PC 꺼놔도 작동.

---

## 2. 전체 구조

```
┌─────────────────────────────────────────────────────┐
│  GitHub 저장소 (인터넷상의 폴더 겸 이미지 서버)         │
│                                                     │
│   images/               ← 사장님이 사진 넣는 곳       │
│   topics.json           ← 글 주제 18개 + 패키지 구성   │
│   threads_auto_post.py  ← 실제 일하는 프로그램        │
│   posted_log.json       ← 게시 기록 (자동 생성)       │
│   .github/workflows/post.yml ← 하루 3회 실행 시간표   │
└─────────────────────────────────────────────────────┘
          │ 정해진 시각마다 GitHub Actions가 실행
          ▼
┌─────────────────────────────────────────────────────┐
│  실행 1회의 흐름 (약 1분 소요)                         │
│                                                     │
│  ① topics.json에서 이번 회차 주제 선택 (순서대로 순환)  │
│  ② 현재 시간대(아침/점심/저녁) 판별                    │
│  ③ Claude API 호출 → MZ 말투 + 댓글 유도 글 생성       │
│     (최근 글 9개를 함께 보내 중복 방지)                │
│  ④ images/ 에서 랜덤 3장 추출 (직전 회차 사진 제외)     │
│  ⑤ Threads API로 게시:                               │
│     사진 3장 각각 등록 → 캐러셀 묶음 생성 →            │
│     35초 처리 대기 → 최종 게시                        │
│  ⑥ posted_log.json에 기록 저장 (최근 50개 유지)        │
│  ⑦ 토큰 갱신 20일 경과 시 자동 갱신 → Secrets에 저장    │
└─────────────────────────────────────────────────────┘
```

### 게시 시간표 (한국시간)

| 회차 | 시각 | 글 분위기 (자동 반영) |
|---|---|---|
| 1회 | 오전 08:00 | 새벽 러닝·아침 사우나·조식 |
| 2회 | 낮 12:30 | 나른한 오후, 주말 계획·피로 회복 |
| 3회 | 저녁 19:00 | 퇴근 후, 막걸리 한 잔·조용한 진안의 밤 |

※ GitHub 사정에 따라 몇 분~수십 분 늦게 실행될 수 있음 (정상)

---

## 3. 열쇠 4개 (GitHub Secrets)

| 이름 | 역할 | 관리 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Claude 글 생성 열쇠 (홍삼빌호텔과 공용 가능) | 잔액 소진 시만 충전 |
| `THREADS_ACCESS_TOKEN` | **@jinan.sauna** 게시 출입증 | 20일마다 자동 갱신·자동 저장 |
| `THREADS_USER_ID` | @jinan.sauna 계정 번호 | 변경 불필요 |
| `GH_PAT` | 갱신된 토큰을 금고에 넣는 열쇠 | 1년 후 만료 → 재발급 |

> ⚠️ 홍삼빌호텔 저장소와 **Secrets 는 별개**입니다. 토큰과 사용자 ID 는 반드시 @jinan.sauna 계정 것을 넣어야 합니다.

---

## 4. 평소 운영 매뉴얼

### 매일 할 일
**없음.** (권장: 하루 1회 @jinan.sauna 에 달린 댓글에 답글 — 알고리즘 노출에 큰 도움)

### 가끔 할 일

**사진 보충** (최소 3장 필수, 20~30장 권장)
1. 저장소 → `images` 폴더 → Add file → Upload files
2. 사진 드래그 → **Commit changes**
3. 규칙: JPG/PNG · 8MB 이하 · 파일명 영문+숫자 (`sauna_01.jpg`, `running_02.jpg`)
4. 사우나·러닝 코스·조식·전통주·객실·천둥이 등 패키지 5요소가 골고루 있으면 좋음

**주제·패키지 구성 수정**
1. 저장소 → `topics.json` → ✏️
2. `"topics": [...]` — 글 주제 / `"package_items": [...]` — 패키지 구성 / `"brand_info"` — 상품 설명
3. 따옴표와 쉼표 형식 유지 → Commit changes

**글 말투 조정**
- `threads_auto_post.py` 안의 `system = f"""..."""` 부분이 글쓰기 지시문
- ⚠️ 들여쓰기 어긋나면 오류 → 자신 없으면 Claude에게 전체 파일 재생성 요청

**시간·횟수 변경**
- `.github/workflows/post.yml` 의 `schedule:` 아래 cron 줄 수정
- cron은 **UTC 기준 = 한국시간 −9시간** (예: 한국 08:00 → `0 23 * * *`)

### 수동으로 즉시 1회 게시하기
저장소 → Actions 탭 → 진안사우나 스레드 자동 포스팅 → **Run workflow**

---

## 5. 비용

| 항목 | 비용 |
|---|---|
| GitHub 저장소·자동 실행 | 무료 |
| Threads API (Meta) | 무료 |
| Claude API (글 생성) | **월 약 2,000~3,000원** (1회 약 25~30원 × 90회) |

---

## 6. 정기 점검 캘린더

| 주기 | 할 일 |
|---|---|
| 매일 | 없음 (댓글 답글 권장) |
| 수시 | 사진 보충, GitHub "Run failed" 메일 확인 |
| 잔액 소진 시 | Claude API 크레딧 충전 |
| **1년 후** | GH_PAT 재발급 → Secrets 교체 |

---

## 7. 문제 해결

오류 확인 경로: **Actions 탭 → 빨간 ❌ 실행 클릭 → post 클릭 → ❌ 단계 펼치기 → `❌ 실행 실패:` 메시지 확인**

| 증상 / 오류 메시지 | 원인 | 해결 |
|---|---|---|
| 글이 **홍삼빌호텔 계정**에 올라감 | 토큰을 호텔 계정으로 발급 | @jinan.sauna 로 로그인 후 토큰 재발급, `THREADS_ACCESS_TOKEN`·`THREADS_USER_ID` 교체 |
| `Cannot parse access token` (code 190) | 토큰이 잘리거나 따옴표·공백 포함 | 장기 토큰 재발급 → 따옴표 제외 전체 복사 → Secret 덮어쓰기 |
| `Invalid OAuth access token` / `Session has expired` | 토큰 만료 | 그래프 API 탐색기에서 재발급 → 1시간 내 장기 토큰 교환 → Secret 교체 |
| `IndentationError` | 파이썬 파일 들여쓰기 어긋남 | 파일 전체를 정상본으로 교체 |
| `이미지가 N장뿐입니다` | images 폴더 사진 3장 미만 | 사진 보충 |
| Status: **Queued** 로 멈춤 | GitHub 대기열 | 몇 분 대기, 10분 넘으면 취소 후 재실행 |
| 스레드에 사진이 안 뜸 | 한글·특수문자 파일명 또는 비공개 저장소 | 영문 파일명 / 저장소 Public 확인 |
| Claude 401/402 오류 | API 키 오류 또는 잔액 소진 | 키 확인 / 크레딧 충전 |
| 갱신 후에도 옛 토큰 사용 | GH_PAT 만료·권한 부족 | GH_PAT 재발급 (Secrets: Read and write) |

### 토큰 수동 재발급 전체 절차
1. 스레드 앱/웹에 **@jinan.sauna 로 로그인** 되어 있는지 확인
2. developers.facebook.com → 도구 → **그래프 API 탐색기** → 도메인 `.threads.net` → 앱 선택 → **Generate Threads Access Token** → 승인
3. 1시간 안에 장기 토큰 교환:
   ```
   https://graph.threads.net/access_token?grant_type=th_exchange_token&client_secret=앱시크릿&access_token=단기토큰
   ```
4. `"access_token"` 값을 **따옴표 빼고** 복사 → Secrets `THREADS_ACCESS_TOKEN` 덮어쓰기
5. Actions → Run workflow 로 테스트

---

## 8. 글 작성 규칙 (프로그램에 내장됨)

- 패키지 6요소(배럴 사우나·토도노이 냉수욕·마이산 러닝·호텔 숙박·딸기막걸리 시음·조식) 중 매 글 1~2가지에 집중
- 게시 시간대에 맞는 분위기 (아침엔 러닝·사우나, 밤엔 막걸리)
- 가격은 홈페이지 기준 "1인 99,000원, 평일 월~목 한정·성수기 제외" 그대로만 (매 글마다 넣지 않음)
- **금지**: 지어낸 할인·이벤트, 의학적 효능 단정(사우나·냉수욕·막걸리), 음주 권장, 호텔이 고기·식재료를 판다는 표현, 노골적 예약 유도
- 막걸리는 "시음", "한 잔 맛보기" 수준으로만 표현
- 홈페이지(www.jinansauna.kr)는 3~4번 중 1번 정도만 언급

---

## 9. 파일별 역할 요약

| 파일 | 역할 | 사장님이 만질 일 |
|---|---|---|
| `images/` | 사진 보관함 겸 이미지 서버 | ⭕ 자주 |
| `topics.json` | 주제·패키지 구성·해시태그·상품 정보 | ⭕ 가끔 |
| `threads_auto_post.py` | 글 생성 + 게시 + 토큰 갱신 | △ 드물게 |
| `.github/workflows/post.yml` | 실행 시간표 (하루 3회) | △ 드물게 |
| `requirements.txt` | 토큰 자동 저장용 라이브러리 | ❌ |
| `posted_log.json` | 게시 기록 (자동) | ❌ |
| `.token_meta.json` | 토큰 갱신 날짜 (자동) | ❌ |
